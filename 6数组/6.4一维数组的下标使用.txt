数组的下标
  -数组的第一个元素到最后一个元素都有一个连续的下标
   下标是从0开始的，即第一个元素的下标是 0
   最后一个元素的下标是数组元素个数-1
   第n个元素的下标为n-1

下标的作用
  -用于访问数组的某一个元素
   格式为 数组名[元素对应下标]
   如有一个数组 int arr[2] = {1,2};
   我想用printf函数打印它的第二个元素2
   则可写出如下代码
   printf("%d",arr[1]); 
   结果即是打印一个2在屏幕上

*注意：本质上 arr[i] 相当于 *(arr+i)