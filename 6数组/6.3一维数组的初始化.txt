数组的初始化：
指的是在创建数组的同时给数组的内容一些合理初始值(初始化)
如	int arr[3] = { 1, 2, 3};
	int arr[] = { 1, 2, 3};

不完全初始化：
即初始化的元素个数小于数组大小。
这时未初始化的元素默认初始化为 0
如 int arr[5] = { 1, 2, 3};
   // 数组内存放的元素为 1 2 3 0 0

*数组如果没有初始化，所有元素均会为随机值
所以最好至少初始化一位，如下
int arr[5] = {0};