数组传参时，为了节省内存空间，传得是数组首元素的地址，而非数组。
所以实际上数组实参在传参过程中退化为指针形参
因此函数并不能求数组形参的元素个数
如
int my_arr_sizeof(int arr[])
{
    return sizeof(arr)/sizeof(arr[0])
}
// error，此时arr不再是数组，而是个 int* 类型的指针，存放数组实参的首元素地址，大小为 4，所以结果恒为1
