数组名是数组首元素地址

两个例外：
1.sizeof(数组名)
计算整个数组大小。
此时数组名表示整个数组

2.&数组名
取出数组的地址，
此时虽然取出的地址和数组首元素地址一样，但是此时取出的是数组的地址，在加减法等操作上和数组首元素地址有区别。
此时数组名表示整个数组
具体可看文件 16.5指针类型的区别

*注意：n维数组的数组名不等于n级指针
如二维数组的数组名的值是一个数组指针的地址
而二级指针的值是一级指针的地址
