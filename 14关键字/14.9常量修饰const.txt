*const 修饰变量，使其变为常(变)量
   -如 const int num = ...; num为常变量
   -但本质为变量，无法在使用常量的地方
    替代常量，仅可视为一种不变变量

*const修饰指针，使其变为常量指针或者指针常量
1.常量指针
如 const int* n;
指针 n 无法通过解引用操作改变其所指向地址对应的变量
但其对应的变量仍可以通过其他方式改变

2.指针常量
如  int* const n;
指针 n 的值无法再改变，不能再指向其它地址
其 n 可以通过解引用操作改变其所指向地址对应的变量