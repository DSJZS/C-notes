内存如何被使用和CPU有关
CPU有32位的和64位的
32位的CPU能产生 2^32 个地址，因此内存被分为2^32 个空间
64位的CPU能产生 2^64 个地址，因此内存被分为2^64 个空间
每个空间占一个字节
(为什么占一个字节，因为如果是bit，那么32位下，一个内存的大小为 2^32bit = 512Mb = 0.5Gb 过于小了，而如果是Byte，则为 2^32byte = 4Gb，比较够用)