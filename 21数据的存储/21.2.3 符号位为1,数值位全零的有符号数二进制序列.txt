如果一个有符号数的二进制序列它的符号位是 1，数值位是 0
那么该有符号数的值不经过计算，直接判断为该类型正数最大值加一取反后的数(为一个绝对值比该类型正数最大值多一的负数)

如 unsigned char类型的 10000000
根据规则，无需任何计算，直接判断其结果
其值为 -128
由此可得 unsigned char类型的取值范围是 -128 ~ 127

以此类推, 32位 和 64位 系统下的 unsigned int 类型取值范围是： -2147483648 ～ 2147483647

*注意：由规则可得，当整形有符号数的最大值加一时会便会得到最小值
