野指针：就是指针指向的位置是不可知(随机的，不正确的，没有明显限制的)的指针

如
1.未初始化的指针变量
	int* p; // 未初始化
	*p = 10; //error
2.指针的越界访问
	int arr[2] = {0};
	int* p = arr;
	*(p+2) = 10; // 越界访问, error
3.指针指向的空间释放
动态内存开辟和释放，函数内局部变量销毁会造成此类情况	

*注意：避免产生野指针，禁止对野指针进行解引用操作

如何规避？
1.指针初始化
2.小心指针越界
3.指针指向空间释放即使置NULL
4.指针使用之前检查有效性