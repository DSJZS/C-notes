上面文件里的指针均为一级指针
一级指针本身作为一个变量，也需要在内存中开辟空间存放，所以其本身也有地址，而二级指针就是用来存放一级指针地址的变量

如	int a = 10;
	int* pa = &a;
	int** ppa = &pa;

解引用操作：二级指针解引用操作后使一级指针的空间，再解引用操作才是一级指针指向的变量在的那个空间

n级指针用来存放n-1级指针的地址
1级指针用来存放变量的地址
n级指针变量的类型中有n个*
n级变量要解引用到变量的空间要n个*

*注意：n维数组的数组名不等于n级指针
如二维数组的数组名的值是一个数组指针的地址
而二级指针的值是一级指针的地址
