&& 为逻辑与操作符
它是一个双目操作符
用于描述“并且”的逻辑

语法： 
表达式1 && 表达式2
当其左右表达式均为真时，即为真

举例：
	int a = 5;
        if( a!=4 && a==5 )
            printf("true");
        else
            printf("false"); 
        // 输出 true