|| 为逻辑与操作符
它是一个双目操作符
用于描述“或者”的逻辑

语法： 
表达式1 && 表达式2
当其左右表达式中至少一个为真时，即为真

举例：
	int a = 3;
        if( a!=4 || a==5 )
            printf("true");
        else
            printf("false"); 
        // 输出 true