单目操作符++会使变量按照其规则自增1(如指针按其大小等)

其分前置与后置
如  int a = 0;
    a++;//后置
    ++a;//前置

前置与后置区别
   -前置是先自增1，后使用变量
    如接上列代码 
    printf("%d\n", ++a);  // 结果为 1
    先使a自增1变为1，再打印a
   -后置是先使用变量，后自增1
    如接上列代码 
    printf("%d\n", a++);  // 结果为 0
    先打印a，后使a自增1变为1