逗号表达式：
exp1,exp2,exp3,...expN

逗号表达式，就是用逗号隔开的多个表达式。
逗号表达式，从左向右依次执行。整个表达式的结果是最后一个表达式的结果

如	int a = 1;
	int b = 2;
	int c = (a > b, a = b + 10, a, b = a + 1);
	printf("a=%d,b=%d,c=%d\n", a, b, c);
	// 输出 12 13 13