得出一个数的开平方数
头文件: math.h

函数声明：
double sqrt(double x);

返回值：
输入数的开平方数

调用格式:
sqrt(x);
x 为数字

说明：输入一个数字，会返回其开平方数(类型为双进度浮点数)

举例：
	double x= 9.8596;
	printf("%lf\n", sqrt(x));
	// 输出 3.140000

注意：其返回指为双精度浮点数，注意精度丢失的问题