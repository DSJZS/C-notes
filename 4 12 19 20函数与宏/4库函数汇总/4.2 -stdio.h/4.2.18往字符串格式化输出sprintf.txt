函数的头文件: stdio.h   or   wchar.h

函数的作用：
发送格式化输出到 str 所指向的字符串。

函数声明：
int sprintf(char *str, const char *format, ...);

参数说明：
str 	--	 这是指向一个字符数组的指针，该数组存储了 C语言字符串。
format	--	 这是字符串，包含了要被写入到字符串 str 的文本。它可以包含嵌入的 format 标签，format 标签可被随后的附加参数中指定的值替换，并按需求进行格式化。

返回值：
如果成功，则返回写入的字符总数，不包括字符串追加在字符串末尾的空字符。如果失败，则返回一个负数。

调用格式:
sprintf( str,  format, ...);

举例：
	char buf[1024] = {0};
	sprintf(buf,"%d %f %s",2,3.14f,"hello_world");
	printf(buf); // 输出 2 3.140000 hello_world

注意事项：
1.str所指向的字符串要有足够的大小去容纳格式化输出的内容
2.如果失败，会返回一个负数

建议：
1.检测返回值

自己编写的版本：
一辈子写不出
该模板应用于 2023.8.27 22:00后编写的函数与宏的介绍( 这句话也是未完成标识，未完成的笔记不应删除这行字 )