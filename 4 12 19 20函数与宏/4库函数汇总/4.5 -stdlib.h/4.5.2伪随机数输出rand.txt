返回一个范围在 0 到 RAND_MAX 之间的伪随机数。
RAND_MAX 是一个常量，它的默认值在不同的实现中会有所不同，但是值至少是 32767。

头文件: stdlib.h

函数声明：int rand(void)
                   无参数

返回值：该函数返回一个范围在 0 到 RAND_MAX 之间的整数值

调用格式: rand()

说明：该函数返回的指是利用随机数种子生成的伪随机值,其随机数种子默认是1，可用srand函数，初始化随机数种子

举例：int x = rand();

该函数返回的是伪随机值