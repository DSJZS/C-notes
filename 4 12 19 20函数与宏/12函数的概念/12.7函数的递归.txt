什么是递归？
 -程序调用自身的编程技巧称为递归
  通俗一点来说就是一个函数自己调用自己

递归的两个必要条件：
1.存在限制条件，当满足这个限制条件的时候，递归便不再继续
2.每次递归调用之后越来越接近这个限制条件
*注意：条件不满足一定会造成栈溢出等问题
*注意：条件满足也可能会造成栈溢出等问题

递归举例
void print(int num)
{
	if (num >= 10)
		print(num / 10);
	printf("%d ", num % 10);
}
int main()
{
	print(10086); //输出 1 0 0 8 6
	return 0;
}